% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survivaleff.R
\name{draw_group_comparison_plot}
\alias{draw_group_comparison_plot}
\title{Draw Group Comparison Plot with Statistical Tests}
\usage{
draw_group_comparison_plot(
  data,
  value_cols,
  eff_col,
  type_labels = NULL,
  y_label = "B cell(\%)",
  y_limit = c(0, 4),
  y_breaks = seq(0, 4, 1),
  palette = c("#0073C2FF", "#EFC000FF"),
  p_label_y = NULL,
  dodge_width = 0.5,
  p_digits = 3
)
}
\arguments{
\item{data}{A data frame containing the input data.}

\item{value_cols}{A character vector of column names representing different timepoints or measurements.}

\item{eff_col}{A character string indicating the column name for grouping (e.g., treatment group).}

\item{type_labels}{Optional named vector to relabel \code{value_cols} in the plot.}

\item{y_label}{Y-axis label for the plot. Default is \code{"B cell(\%)"}.}

\item{y_limit}{A numeric vector of length 2 setting Y-axis limits. Default is \code{c(0, 4)}.}

\item{y_breaks}{A numeric vector specifying the breaks for Y-axis. Default is \code{seq(0, 4, 1)}.}

\item{palette}{A vector of colors used for plotting different groups. Default is a blue-yellow palette.}

\item{p_label_y}{Optional numeric value or vector to control vertical position of p-value labels.}

\item{dodge_width}{Width for dodging points and error bars. Default is 0.5.}

\item{p_digits}{Number of decimal places to show in p-values. Default is 3.}
}
\value{
A list with the following components:
\item{plot}{A \code{ggplot2} object showing the comparison plot.}
\item{results}{A data frame of raw statistical test results.}
\item{formatted_results}{A formatted table of results with significance, normality, and variance assumptions.}
\item{gt_table}{A \code{gt} object showing a styled result table (if \code{gt} is installed).}
}
\description{
This function generates a group comparison plot for multiple timepoints or variables.
It performs normality and variance homogeneity tests (Shapiro and Levene), then
applies appropriate statistical comparisons (t-test or Wilcoxon test), and summarizes results in a GT table.
}
